{
  "name": "@localizesh/processor-txt",
  "repository": "localizesh/processor-txt",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "localize-processor-txt": "dist/bin.js"
  },
  "files": [
    "/dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc --project tsconfig.build.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@localizesh/sdk": "^1.0.0",
    "eol": "^0.10.0",
    "hastscript": "^9.0.1",
    "unist-builder": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.9",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
